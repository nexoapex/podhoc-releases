# Release v1.1.0 — seo:  browser-render every sitemap via shared XSL pretty-pri

## Highlights

- This release includes new features, improvements, and fixes across various areas.

## What's New
- seo:  browser-render every sitemap via shared XSL pretty-printer
- dns:  prod www.podhoc.com 301-redirects to apex
- review-accounts:  add prod support via ENVIRONMENT=prod opt-in
- v1.1.0:  launch-blocker fixpack + tf consolidation (40→29) + docs overhaul
- launch-blockers:  anon-leak + credit refresh + cost warnings + covers + welcome creds + auth-race

## Bug Fixes
- og-render:  fetch SPA shell from S3 — Lambda@Edge response events have no body field
- podcast-worker:  auto_publish stale podcast_script_id column ref
- seo:  restore SSR header handoff + sync Discover filters to URL
- seo:  restore SSR header handoff + sync Discover filters to URL
- discover:  hide Connect YouTube CTA for anonymous sidebar visitors

