# Release v1.1.3 — user-webapp:  mint + persist SSO refresh token to fix silent

## Highlights

- This release includes new features, improvements, and fixes across backend, user-app, auth, infrastructure, database, shared-components, website.

## What's New
- user-webapp:  mint + persist SSO refresh token to fix silent renew
- smartwatch:  Garmin FR965 e2e — CBR backend + play/now-playing app
- cover-image-generator:  generate aspect-ratio cover variants

## Bug Fixes
- auth:  drop offline_access — Cognito rejects it (keep cookie writeback)
- auth-edge:  bodyless 401/403 for asset auth failures (no HTML-as-JS)
- presentational-web:  retype /pricing/ JSON-LD Product -> Service
- user-webapp:  never blank-screen on a failed lazy route chunk
- garmin:  fetch over direct HTTP (not phone messaging) so sync works on iOS

