# Release v1.1.0 — seo:  browser-render every sitemap via shared XSL pretty-pri

## Highlights

- This release includes new features, improvements, and fixes across backend, infrastructure, website.

## What's New
- seo:  browser-render every sitemap via shared XSL pretty-printer

## Bug Fixes
- podcast-worker:  auto_publish stale podcast_script_id column ref
- seo:  restore SSR header handoff + sync Discover filters to URL
- sidebar:  bottom Sign In CTA must full-reload like #513 did for menu
- sidebar:  anon Sign In nav must full-reload so auth-edge fires
- auth-edge:  restore main-bundle gate as PROD-bypass / DEV-magic-cookie

